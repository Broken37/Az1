# بخش سازنده‌ی مرج ریکوئست
## توضیحات
### مشکل
<!--
   در اینجا لینک مشکل داخل بورد و اگر لازم است توضیحی درباره‌ی آن را بیاورید.
-->

فاقد توضیحات :(

### راه حل
<!--
  در اینجا توضیحاتی درباره‌ی نحوه‌ی راه حل، لینک دیزاین‌داک در صورت وجود، توضیحات خاص درباره‌ی معماری اگر لازم است و هر توضیح دیگری که برای راه حل و پیاده‌سازی به نظرات لازم است را بنویسید.
-->

فاقد راه حل :(

## چک‌لیست
### بررسی ورژن‌های قبلی
   - [ ] این تغییر باعث ایجاد تغییر در ورژن‌های قبلی بلد نمی‌شود.
   - [ ] این تغییر روی ورژن‌های قبلی بلد تست شده و همه‌چیز درست است.
### بررسی مایگریشن‌های پایگاه داده
   - [ ] این تغییر فاقد تغییر روی پایگاه داده است.
   - [ ] این تغییر شامل تغییر روی پایگاه داده است که مایگریشن‌های آن روی استیجینگ تست شده و بدون داون‌تایم قابل انجام شد.
   - [ ] این تغییر شامل تغییر روی پایگاه داده است اما نیازی به تست برروی استیجینگ ندارد.
### سایر چک‌لیست‌ها
 - [ ] این تغییر روی آخرین نسخه‌ی اپلیکیشن بلد تست شده و همه‌چیز درست است.
 - [ ] این تغییر برروی استیچینگ تست شده.
 - [ ] برای همه‌ی کدهای جدید پیاده‌سازی شده یونیت تست نوشته شده.
 - [ ] تغییرهای داکیومنتیشن (شامل README و ApiDoc) انجام شده.

# بخش ریویو کننده
## چک لیست
 - [ ] در این مرج ریکوئست قواعد کلی پروژه به خوبی رعایت شده.
 - [ ] طراحی و دیزاین راه حل انتخاب شده مناسب است و راه حل مناسبی انتخاب شده.
 - [ ] این تغییر شامل Over-Engineering نیست و پیچیدگی اضافه به سیستم وارد نکرده.
 - [ ] قواعد تمیزی کد به خوبی رعایت شده.
 - [ ] این تغییرات سلامتی سیستم را به خطر نمی‌اندازد.
